services:
  app:
    image: shawiizz-portfolio-frontend
    build:
      context: ../..
      dockerfile: Dockerfile.app
    restart: always
    ports:
      - "{{ current.env.app_external_port }}:3000"