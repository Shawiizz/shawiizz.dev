project_name: self-website

health_checks:
  enabled: true
  on_failure: "rollback"
  endpoints:
    - name: "Main Application"
      url: "https://{{ current.env.main_domain }}/"
      expected_status: 200
      timeout: 30
      retries: 3
      retry_delay: 5
